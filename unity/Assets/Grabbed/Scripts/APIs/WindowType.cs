// Vendored extract from reference `APIs/WindowManager.cs` (WindowManager itself is
// a Phase-7 platform backend, deferred). Needed by grabbed Settings/SaveLoadHandler.cs.

public enum WindowType
{
    Normal = 0,
    Dock = 1,
    Desktop = 2
}
